[pytest]
# Suppress known third-party DeprecationWarnings that we cannot fix upstream.
# The lxml/scrapling warning fires on every HTTP request and is noise.
filterwarnings =
    ignore:.*strip_cdata.*:DeprecationWarning:lxml
    ignore:.*strip_cdata.*:DeprecationWarning
