---
title: "Quickstart"
description: "First scrape on a self-hosted instance."
---

Prefer the full guide: [Self-host quickstart](/self-host).

## 1. Run locally

```bash
cp .env.example .env
docker compose up --build
```

Open http://localhost:8080 → register → create an API key (`fs_…`).

## 2. Scrape

```bash
curl -X POST "http://localhost:8000/v1/scrape" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com",
    "formats": ["markdown"],
    "onlyMainContent": true
  }'
```

## Next

<CardGroup cols={2}>
  <Card title="Connect provider" icon="key" href="/connect-provider">
    Scrape.do / Scrapfly env or BYOK.
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/introduction">
    scrape · crawl · map
  </Card>
</CardGroup>
